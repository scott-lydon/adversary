# Caddyfile fragment for the Adversary dashboard.
#
# Installed at /etc/caddy/Caddyfile.d/adversary.caddy on the Hetzner host
# (or appended to /etc/caddy/Caddyfile if the import directive is not
# present in the main file). Caddy auto-reload picks the change up.
#
# Why sslip.io: any *.<dashed-ip>.sslip.io resolves to that IP without DNS
# setup, so we get a real Let's Encrypt cert with zero domain ownership
# friction. Swap the hostname when a real domain is added.

adversary.5-161-253-237.sslip.io {
    # Caddy's auto-HTTPS issues a Let's Encrypt cert for this hostname on
    # first request. ACME HTTP-01 succeeds because Caddy listens on 80
    # and 443 and sslip.io resolves to this box.

    encode zstd gzip

    # Basic-Auth gate: admin / gauntlet-g5 (cohort-demo credentials,
    # NOT secret). The bcrypt hash below was generated with
    # `bcrypt.gensalt(rounds=10)` and verified locally before commit; the
    # plaintext is intentionally public so graders can reach the
    # dashboard with zero side-channels.
    #
    # Rotated 2026-05-14 from `pass` to `gauntlet-g5`. The previous
    # password was so common that Mac Keychain auto-filled it without
    # any visible prompt, which made it look like the gate was off. A
    # cohort-specific password forces the prompt to appear at least
    # once.
    #
    # Safari quirk note: when the gate was on with a randomly-generated
    # password earlier, Safari re-prompted on every internal navigation.
    # That behavior is a Safari path-cache bug, not a server-side issue
    # (Caddy emits a stable WWW-Authenticate realm = the host). Workaround
    # for graders on Safari: use Chrome/Firefox, or check "Remember this
    # password" the first time. Re-removing the gate is a one-line edit.
    basic_auth {
        admin $2b$10$NEDZV1PEJfRvcx9JY5bWWem9GZcW.SmkQ5SbfquA5RExAqobt1ede
    }

    reverse_proxy 127.0.0.1:8765 {
        header_up X-Real-IP {remote_host}
        header_up X-Forwarded-Proto {scheme}
        # Long-running /scan POSTs can take >60s while the orchestrator
        # waits on the Red Team LLM. Raise the upstream read timeout so
        # Caddy does not 504 mid-campaign.
        transport http {
            read_timeout 5m
            write_timeout 5m
        }
    }

    # Lock down the audit-tamper endpoint to localhost only; it lets you
    # rewrite history and must never be reachable through the proxy.
    @forbidden path /audit/tamper*
    respond @forbidden 403
}
